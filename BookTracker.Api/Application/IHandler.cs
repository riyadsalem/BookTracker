namespace BookTracker.Api.Application;

// IHandler doesn't contain any members.
// It simply marks a class as a handler, allowing Reflection to discover
// and register it automatically during application startup.
public interface IHandler; // Marker Interface